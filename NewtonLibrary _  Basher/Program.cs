using Microsoft.VisualBasic;
using NewtonLibrary____Basher;
using NewtonLibrary____Basher.Data;
using NewtonLibrary____Basher.modells;
namespace NewtonLibrary____Basher
   
{
     class Program
     {
        static void Main(string[] args)
        {
            DataAccess data = new DataAccess();

            //data.CreateFiller(); // Skapar 10 random av varje klass
            //data.Clear(); // Rensar allt som finns på databasen
            //data.AddLoanCardToPerson(109); //Lägger till ett kort till användaren som har (i detta fall) id 3
            //var authorNames = new List<string> { "Moppskaft" };
            //data.AddBookToDatabase("CV Bok",authorNames); // Du skapar en sträng(bok), Säger att en eller flera personer skrivit boken
            //data.MarkBookAsNotLoaned(11); //Om den är kopplad till ett id tas den bort, den är inte lånad längre
            //data.AddPersonToDatabase("Messi", "Ronaldo");
            //data.AddAuthorToDatabase("1","2");
            //var authorNames = new List<string> {"Husby blabla" };
            //data.AddBookToDatabase("Newton",authorNames,1);
            //data.RemovePerson(110);
            //data.Removebook(111);
            //data.Removeauthor(11);
            //data.AddBookIdToPersonLoanCard(109, 110);
            
            //data.ReturnBook(110);

            //data.seed();

            /* Meny
            Console.WriteLine("1. List Books");
            Console.WriteLine("2. Borrow Books");
            Console.WriteLine("3. Return Books");

            Console.WriteLine("4. Exit");

            int input = int.Parse (Console.ReadLine());

            switch (input)
            { 
                //case 1;
                    


            }
            */
        }



    }
}
    